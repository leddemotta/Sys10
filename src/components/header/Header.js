import React from 'react';
import { Tabs } from 'antd';

const { TabPane } = Tabs;
const callback = (key) => { }

const Header = () => {
    return (
        <header>
           
        </header>
    )
}

export default Header;